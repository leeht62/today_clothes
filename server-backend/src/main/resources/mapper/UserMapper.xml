<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.server.mapper.UserMapper">
    <select id="findAll" resultType="com.server.today_clothes.VO.User">
        SELECT id, user_code, password, name
        FROM users

    </select>
    <select id="findById" resultType="com.server.today_clothes.VO.User" parameterType="long">
        SELECT id, user_code, password, name
        FROM users
        WHERE id = #{id}
    </select>
    <insert id="save" parameterType="com.server.today_clothes.VO.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (user_code, password, name)
        VALUES (#{userCode}, #{password}, #{name})
    </insert>
    <update id="update" parameterType="com.server.today_clothes.VO.User">
        UPDATE users
        SET
            user_code = #{userCode},
            password = #{password},
            name = #{name}
        WHERE id = #{id}
    </update>
    <delete id="deleteById" parameterType="long">
        DELETE FROM users WHERE id = #{id}
    </delete>

</mapper>