<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.server.mapper.CommentMapper">
    <select id="findAll" resultType="com.server.today_clothes.VO.Comment">
        SELECT id, user_id, weather_id, comment
        FROM comment
    </select>
    <select id="findById" resultType="com.server.today_clothes.VO.Comment" parameterType="long">
        SELECT id, user_id, weather_id, comment
        FROM comment
        WHERE id = #{id}
    </select>
    <insert id="save" parameterType="com.server.today_clothes.VO.Comment"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comment (user_id, weather_id, comment)
        VALUES (#{userId}, #{weatherId}, #{comment})
    </insert>
    <update id="update" parameterType="com.server.today_clothes.VO.Comment">
        UPDATE comment
        SET
            user_id = #{userId},
            weather_id = #{weatherId},
            comment = #{comment}
        WHERE id = #{id}
    </update>
    <delete id="deleteById" parameterType="long">
        DELETE FROM users WHERE id = #{id}
    </delete>



</mapper>